using System;

// creates a public class 'PromptGenerator'
public class PromptGenerator
{
    {   
        // creates member variable
        public string _prompts(void);

        // Function that generates a prompt when given code 'Prompt();'
        public(void) PromptGenerator()
        {
            
        }
    }
}