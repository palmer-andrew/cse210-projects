using System;

// creates a public class 'Entry'
public class Entry
{
    // creates member variables
    public string _prompt(string);
    public string _entry(string);
    public string _date(string);

    // Function that generates a prompt when given code 'WriteEntry();'
    public(void) WriteEntry()
    {

    }
    
    // Function that generates a prompt when given code 'EntryDisplay();'
    public(void) EntryDisplay()
    {

    }
}