using System;

public class Journal
{
    // creates member variables
    public string _journalOwner(string);

    // Function that generates a prompt when given code 'AddEntry();'
    public(void) AddEntry(string)
    {

    }
    // Function that generates a prompt when given code 'SaveJournal();'
    public(string) SaveJournal(file)
    {

    }
    // Function that generates a prompt when given code 'LoadJournal();'
    public(string) LoadJournal(file)
    {

    }
    // Function that generates a prompt when given code 'CloseJournal();'
    public(void) SaveJournal(void)
    {

    }
    // Function that generates a prompt when given code 'DisplayJournal();'
    public(void) DisplayJournal(void)
    {

    }
}